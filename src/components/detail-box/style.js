import { StyleSheet } from "react-native";

export const local = StyleSheet.create({
  attrRow: {
    flexDirection: 'row',
  },
  keyName: {
    marginRight: 10,
  },
  value: {
    flex: 1,
  }
});
